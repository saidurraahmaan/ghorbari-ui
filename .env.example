VITE_API_BASE_URL=http://localhost:8081/api

# Multi-tenant configuration
# Default tenant for localhost development
VITE_DEFAULT_TENANT=demo

# Optional: Base domain for production (only needed for complex subdomain structures)
# VITE_BASE_DOMAIN=example.com
